function cloze(text, cloze) {
  if (text === undefined || cloze === undefined) {
    console.log("error, youre missing something");
  } else {
    this.cloze = cloze;
    this.partial = text;
    this.fullText = fullText;
  }
}

module.exports = ClozeCard;
